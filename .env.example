# Publishable app key (`pk_...`) used as Pollinations BYOP client_id / app attribution.
# This is NOT the user's spend key.
NEXT_PUBLIC_POLLINATIONS_APP_KEY=

# Optional development-only mock mode for local scaffolding. Disabled for runtime BYOP.
POLLINATIONS_TEXT_MODEL=openai
POLLINATIONS_ALLOW_MOCK=false
